<UserControl x:Class="DidiDerDenker.BirdsEyeView.Controls.SmallCalenderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DidiDerDenker.BirdsEyeView.Controls"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d" 
             Height="350"
             Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <syncfusion:CalendarEdit Grid.Row="0" 
                                 Height="Auto" 
                                 Width="Auto" AllowYearEditing="True" SelectionRangeMode="WholeColumn"/>

        <syncfusion:CalendarEdit Grid.Row="2" 
                                 Height="Auto" 
                                 Width="Auto"/>
    </Grid>
</UserControl>
